name: flutter_rive
description: "A new Flutter project."
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Add latest rive dependency
  rive: ^0.14.0-dev.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  # Add example assets
  assets:
    - assets/animations/
    - assets/compressed/
    - assets/fonts/
    - assets/images/